# 运行端口
PORT=3000

# OpenAI 标准接口配置
# API Key（必填）
OPENAI_API_KEY=
# 模型名称（必填，如 gpt-4o-mini、gpt-4o、gpt-3.5-turbo 等）
OPENAI_MODEL=
# 基础地址（可选，默认 https://api.openai.com）
OPENAI_BASE_URL=https://api.openai.com
# 路径（可选，默认 /v1/chat/completions）
OPENAI_API_PATH=/v1/chat/completions

# 系统提示词（若留空，客户端也可传入）
SYSTEM_PROMPT=

# 站点标题（仅用于参考）
SITE_TITLE=户晨风聊天


